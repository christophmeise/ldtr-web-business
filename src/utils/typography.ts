import Typography from 'typography';
import noriegaTheme from 'typography-theme-noriega';

/* noriegaTheme.overrideThemeStyles = ({ rhythm }, options) => ({
    h1: {
        fontSize: ,
    },
}); */

const typography = Typography(noriegaTheme);

export default typography;
